global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/alerts/*.yml

scrape_configs:
  - job_name: battle
    metrics_path: /metrics
    static_configs:
      - targets: [prod-battle:8000]
