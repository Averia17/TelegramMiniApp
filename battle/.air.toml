root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/battle-server ."
bin = "./tmp/battle-server"
include_ext = ["go"]
exclude_dir = ["tmp"]
delay = 300
stop_on_error = true
send_interrupt = true
kill_delay = "500ms"

[log]
time = true

[misc]
clean_on_exit = true
